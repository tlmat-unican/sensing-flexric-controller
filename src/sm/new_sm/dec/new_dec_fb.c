/*
 * Licensed to the OpenAirInterface (OAI) Software Alliance under one or more
 * contributor license agreements.  See the NOTICE file distributed with
 * this work for additional information regarding copyright ownership.
 * The OpenAirInterface Software Alliance licenses this file to You under
 * the OAI Public License, Version 1.1  (the "License"); you may not use this file
 * except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *      http://www.openairinterface.org/?page_id=698
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 *-------------------------------------------------------------------------------
 * For more information about the OpenAirInterface (OAI) Software Alliance:
 *      contact@openairinterface.org
 */


#include "new_dec_fb.h"

#include <assert.h>

new_event_trigger_t new_dec_event_trigger_fb(size_t len, uint8_t const ev_tr[len])
{
  assert(0!=0 && "Not implemented");
  assert(ev_tr != NULL);
  new_event_trigger_t avoid_warning;
  return avoid_warning;
}

new_action_def_t new_dec_action_def_fb(size_t len, uint8_t const action_def[len])
{
  assert(0!=0 && "Not implemented");
  assert(action_def != NULL);
  new_action_def_t avoid_warning;
  return avoid_warning;
}

new_ind_hdr_t new_dec_ind_hdr_fb(size_t len, uint8_t const ind_hdr[len])
{
  assert(0!=0 && "Not implemented");
  assert(ind_hdr != NULL);
  new_ind_hdr_t avoid_warning;
  return avoid_warning;
}

new_ind_msg_t new_dec_ind_msg_fb(size_t len, uint8_t const ind_msg[len])
{
  assert(0!=0 && "Not implemented");
  assert(ind_msg != NULL);
  new_ind_msg_t avoid_warning;
  return avoid_warning;
}

new_call_proc_id_t new_dec_call_proc_id_fb(size_t len, uint8_t const call_proc_id[len])
{
  assert(0!=0 && "Not implemented");
  assert(call_proc_id != NULL);
  new_call_proc_id_t avoid_warning;
  return avoid_warning;
}

new_ctrl_hdr_t new_dec_ctrl_hdr_fb(size_t len, uint8_t const ctrl_hdr[len])
{
  assert(0!=0 && "Not implemented");
  assert(ctrl_hdr != NULL);
  new_ctrl_hdr_t avoid_warning;
  return avoid_warning;
}

new_ctrl_msg_t new_dec_ctrl_msg_fb(size_t len, uint8_t const ctrl_msg[len])
{
  assert(0!=0 && "Not implemented");
  assert(ctrl_msg != NULL);
  new_ctrl_msg_t  avoid_warning;
  return avoid_warning;
}


new_ctrl_out_t new_dec_ctrl_out_fb(size_t len, uint8_t const ctrl_out[len]) 
{
  assert(0!=0 && "Not implemented");
  assert(ctrl_out!= NULL);
 new_ctrl_out_t  avoid_warning;
  return avoid_warning;
}

new_func_def_t new_dec_func_def_fb(size_t len, uint8_t const func_def[len])
{
  assert(0!=0 && "Not implemented");
  assert(func_def != NULL);
 new_func_def_t  avoid_warning;
  return avoid_warning;
}

